// Placeholder database connector. Replace with Prisma / Mongoose / MongoDB client as needed.
export async function connectDB() {
  // e.g. initialize Prisma client or MongoClient here
  // noop for template
  return;
}
